import { InterceptInterceptor } from './intercept.interceptor';

describe('InterceptInterceptor', () => {
  it('should be defined', () => {
    expect(new InterceptInterceptor()).toBeDefined();
  });
});
